% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_accented_characters.R
\name{replace_accented_characters}
\alias{replace_accented_characters}
\title{Replace Accented Characters with Non-Accented Characters in a Dataframe}
\usage{
replace_accented_characters(df)
}
\arguments{
\item{df}{The input dataframe.}
}
\value{
A new dataframe with accented characters
replaced by non-accented characters in the respective
columns.
}
\description{
This function replaces accented characters with their
non-accented counterparts in all columns that contain
strings of a dataframe. Contains accents and special
characters common across European languages.
}
\examples{
# Example dataframe
df <- data.frame(
  Name = c("Café", "Héllo", "Ëxample"),
  Age = c(25, 30, 35),
  City = c("México", "São Paulo", "Ñuñoa")
)

Replace accented characters in all columns containing
strings
df_modified <- replace_accented_characters(df)
print(df_modified)

}
